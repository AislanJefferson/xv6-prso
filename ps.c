#include "types.h"
#include "stat.h"
#include "user.h"
#include "fs.h"
	
int main( int argc, char *argv[ ] )
	//decodifica args
	
	int prio = ...;
	char path_programa[];
	args_programa
	//

	int pid = getpid();
	setpriority(pid, prio);
	exec(path, args_prorama);
	exit();
}
